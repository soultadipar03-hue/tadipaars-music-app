# Backend API URL
# Leave empty when running on Replit (Vite proxy handles /api → localhost:3000)
# Set to your Render backend URL when deploying separately
VITE_API_URL=https://your-backend.onrender.com
